// Alter box
// user needs to click button to remove
alert('hello');

const message = 'Hello There!';
alert(message);

// Prompt Box
// allows user to submit a response, that then can be stored

const name = prompt('What is your name?');
console.log('Name entered:', name);

// Confirm Box
// allows user to confirm or deny a request

const choice = confirm('True or False?');
console.log('Choice value:', choice);
